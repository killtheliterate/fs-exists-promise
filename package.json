{
  "name": "fs-exists-promise",
  "version": "1.0.0",
  "main": "dist/index.js",
  "description": "Like fs.exists(), but a promise",
  "bugs": {
    "url": "https://github.com/killtheliterate/fs-exists-promise/issues"
  },
  "homepage": "https://github.com/killtheliterate/fs-exists-promise",
  "repository": {
    "type": "git",
    "url": "https://github.com/killtheliterate/fs-exists-promise.git"
  },
  "contributors": [
    "Garrett Dawson <killtheliterate@gmail.com> (http://thedrearlight.com/)"
  ],
  "scripts": {
    "build": "rm -rf ./dist && babel src --out-dir dist",
    "docs": "./node_modules/.bin/downdoc \"src/**/*.js\" docs",
    "prepublish": "rm -rf ./dist && babel src --out-dir dist",
    "pretest": "rm -rf ./dist && babel src --out-dir dist",
    "test": "./node_modules/.bin/mocha test"
  },
  "devDependencies": {
    "babel-cli": "^6.3.15",
    "babel-core": "^6.3.15",
    "babel-preset-es2015": "^6.3.13",
    "chai": "^4.2.0",
    "downdoc": "^4.0.0",
    "mocha": "^2.3.4"
  }
}
